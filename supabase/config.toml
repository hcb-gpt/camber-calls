# Supabase project configuration
# https://supabase.com/docs/guides/cli/config

project_id = "rjhdwidddtfetbwqolof"

[functions.process-call]
verify_jwt = false

[functions.segment-call]
# segment-call is internal-only; auth is enforced via X-Edge-Secret in callers
verify_jwt = false

[functions.segment-llm]
# segment-llm uses X-Edge-Secret only (no JWT)
verify_jwt = false
